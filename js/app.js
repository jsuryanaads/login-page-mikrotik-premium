const form=document.getElementById("loginForm");const message=document.getElementById("message");const packages=document.getElementById("packages");const showPackages=document.getElementById("showPackages");showPackages.addEventListener("click",()=>{packages.hidden=!packages.hidden;showPackages.textContent=packages.hidden?"BELI PAKET INTERNET":"SEMBUNYIKAN PAKET"});form.addEventListener("submit",e=>{e.preventDefault();const u=document.getElementById("username").value.trim(),p=document.getElementById("password").value.trim();if(!u||!p)return;message.textContent="Prototype: login diterima. Integrasi MikroTik akan ditambahkan pada tahap berikutnya."});document.querySelectorAll("[data-package]").forEach(btn=>btn.addEventListener("click",()=>{message.textContent=`Prototype: paket ${btn.dataset.package} dipilih. Payment Gateway akan dihubungkan pada tahap berikutnya.`;document.getElementById("username").focus()}));
