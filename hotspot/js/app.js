const form=document.getElementById("loginForm");const message=document.getElementById("message");const packages=document.getElementById("packages");const showPackages=document.getElementById("showPackages");showPackages.addEventListener("click",()=>{packages.hidden=!packages.hidden;showPackages.textContent=packages.hidden?"BELI PAKET INTERNET":"SEMBUNYIKAN PAKET"});document.querySelectorAll("[data-package]").forEach(btn=>btn.addEventListener("click",()=>{message.textContent=`Paket ${btn.dataset.package} dipilih. Payment Gateway akan dihubungkan pada tahap berikutnya.`}));
